cd backend
npm install
cd ../client
npm install
cd ../image-analysis
bash setup.sh
cd ../
# docker-compose -f docker-compose.dev.yml up -d
# now go open the backend and the image-analysis with vs-code based IDE, run the debugger and start coding :)
